<nav role="navigation" aria-label="Help">
  <h2 class="h4 mb-4"><%= t("decidim.menu.help") %></h2>
  <ul class="space-y-4 break-inside-avoid">
    <% topics.each do |topic| %>
      <%= topic_item(topic, class: "font-semibold underline") %>
    <% end %>
  </ul>
</nav>
